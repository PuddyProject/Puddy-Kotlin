server:
  port: 8080

spring:
  profiles:
    active: local
  data:
    redis:
      host: redis
      port: 6379
      url:


  jpa:
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}

